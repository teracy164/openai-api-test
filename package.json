{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "APP_MODE=production npm run generate && mkdir ./docs/openai-api-test && mv ./docs/_nuxt ./docs/openai-api-test",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@types/marked": "^5.0.0",
    "@types/node": "^18",
    "nuxt": "^3.4.1",
    "sass": "^1.62.1"
  },
  "dependencies": {
    "marked": "^5.0.3",
    "openai": "^3.2.1"
  }
}
